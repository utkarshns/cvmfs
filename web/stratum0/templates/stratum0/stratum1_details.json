{
    "status"            : "ok",
    "name"              : "{{ stratum1_ref.name }}",
    "id"                : {{ stratum1_ref.id }},
    "fqrn"              : "{{ stratum1.manifest.repository_name }}",
    "version"           : "{{ stratum1.version }}",
    "revision"          : "{{ stratum1.manifest.revision }}",
    "stratum0_revision" : "{{ stratum0.manifest.revision }}",
    "rest_api"          : "{{ stratum1.has_rest_api }}",
    "replicating"       : "{{ stratum1.replicating }}",
  {% if repo.last_replication|date:"Y" != '1970' %}
    "last_replication"  : "{{ stratum1.last_replication.isoformat }}"
  {% else %}
    "last_replication"  : "unknown"
  {% endif %}
}